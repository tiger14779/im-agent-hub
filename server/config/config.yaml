server:
  port: 8080
  jwt_secret: "im-agent-hub-jwt-secret-2024"

database:
  host: postgres
  port: 5432
  user: imhub
  password: imhub2024
  dbname: imhub

admin:
  username: "admin"
  password: "admin123"

cleanup:
  enabled: true
  retention_days: 45
  cron: "0 3 * * *"

voice_relay:
  secret: "im-agent-hub-voice-relay-secret-change-me"
  relay_ws_url: ""  # empty = self (use incoming request Host); set to wss://jp.domain.com/api/call/audio on non-JP servers
